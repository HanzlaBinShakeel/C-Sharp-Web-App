@model List<FinanceApp.Models.JournalEntry>
@{
    ViewData["Title"] = "Contra Vouchers";
}

<div class="row">
    <div class="col-xl-12">
        <div class="page-header">
            <h2 class="pageheader-title">Contra Vouchers</h2>
        </div>
    </div>
</div>

<div class="row">
    <div class="col-xl-12">
        <div class="card">
            <div class="card-header">
                <a href="@Url.Action("Create")" class="btn btn-primary">Create New Contra</a>
            </div>
            <div class="card-body">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Voucher Number</th>
                            <th>Date</th>
                            <th>Amount</th>
                            <th>Reference</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var contra in Model)
                        {
                            <tr>
                                <td>@contra.EntryNumber</td>
                                <td>@contra.EntryDate.ToString("dd/MM/yyyy")</td>
                                <td>@contra.TotalDebit.ToString("C")</td>
                                <td>@contra.ReferenceNumber</td>
                                <td>
                                    <a href="@Url.Action("Details", new { id = contra.EntryID })" class="btn btn-sm btn-info">View</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

